{
  "type": "postgres",
  "host": "localhost",
  "port": "5432",
  "username": "postgres",
  "password": "root",
  "database": "ideas",
  "synchronize": true,
  "logging": true,
  "entities": ["src/modules/**/*.entity.{ts,js}", "dist/**/*.entity{.ts,.js}"],
  "migrations": ["src/migrations/**/*.{ts,js}"],
  "cli": {
    "entitiesDir": "src/modules",
    "migrationsDir": "src/migrations"
  }
}
